# Project Instructions

Always use Context7 for any task related to React Bits components, animations, or API usage.

When the task involves React Bits, use the Context7 library:
`/websites/reactbits_dev`

Prefer current examples and API details from Context7 over model memory.

If React Bits guidance conflicts with existing project conventions, preserve the project's architecture and coding style while adapting the component.
